describe('unit > cli', () => {
  it('Displays help text if called with the help flag.', function () {});

  it('Takes an input argument', function () {});

  it('Reads from the file at path given to the file argument', function () {});

  it('Notifies the user if the file doesn\'t exist', function () {});

  it('Passes arguments to brunfaick', function () {});
});
